<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sourcey.materiallogindemo" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Dark" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name="com.sourcey.materialloginexample.item"
            android:label="@string/title_activity_item" >
        </activity>
        <activity
            android:name=".BillsActivity"
            android:label="@string/title_activity_bills"
            android:theme="@style/AppTheme.Dark" >
        </activity>
        <activity
            android:name=".ContactsActivity"
            android:label="@string/title_activity_phones"
            android:theme="@style/AppTheme.Dark">
        </activity>

        <activity
            android:name=".DetailsActivity"
            android:label="@string/title_activity_phonesdt"
            android:theme="@style/AppTheme.Dark">
        </activity>

    </application>

</manifest>
